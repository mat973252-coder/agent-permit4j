# 首次独立接入记录

状态：待安排真实试用。2026-09-11，用户明确选择“暂未确定，先完成示例和试用说明”。
2026-09-22，用户再次确认暂无试用者；试用继续保持待安排。
同日后续决定：用户明确暂缓试用，允许维护者自验通过后发布 0.5.0；本记录仍未测量，不作为已完成试用，也不再阻塞本次发布。
本页的空白项不是成功记录；维护者或代理运行的验收不得填成独立开发者反馈。

## 给试用者的说明

目标是把你已有的三个 Spring AI 方法接入 SDK：一个查询、一个业务预览、一个需要审批的写操作。
先保存原始签名，保留 DTO、集合和代理形式；如果它们不受支持，就记录阻塞，不要先改成示例形状。
使用本地测试数据和替身执行写操作，不接生产接口。

1. 记录 Java、Spring AI、Spring Boot、操作系统以及 SDK 的版本/提交。
2. 按 [独立消费示例](../../examples/spring-ai-adoption/README.md) 安装源码制品并运行示例。
3. 用你的三个方法替换示例工具，显式配置应用授权、审批、幂等、审计和可信上下文。
4. 验证无审批不写入、精确审批后只执行一次、同 key 重试不重复、改变参数和租户被拒绝。
5. 记录卡住的位置、错误码、查阅的文档，以及每一次维护者提示。遇到不支持签名时保留最小复现。

试用者应未参与 SDK 实现。若由项目维护者在另一工程试用，应标为维护者接入，不能当作独立验证。
建议体验目标：首次成功调用 30 分钟，三个方法接入 2 小时；这是待测目标，下载等待单列。

## 待填写

| 项目 | 实际记录 |
| --- | --- |
| 试用日期、试用者是否参与过 SDK 实现 | 待填写 |
| SDK 提交/版本；源码安装或公共仓库消费 | 待填写 |
| Java / Spring AI / Spring Boot / 操作系统 | 待填写 |
| 原始三个方法签名，是否经过 Spring 代理 | 待填写 |
| 依赖下载等待时间 | 待填写 |
| 首次成功调用耗时，不含下载 | 待填写 |
| 三个方法完成耗时，不含下载 | 待填写 |
| 额外依赖、配置、业务代码改动 | 待填写 |
| 每次维护者提示及其耗时 | 待填写 |
| 未支持签名、错误码、最小复现 | 待填写 |
| 审批/幂等/身份边界实际结果 | 待填写 |
| 最终成功或仍被阻塞 | 待填写 |

## 已完成的自动化基线

2026-09-11 的维护者执行：独立消费工程 10 项测试通过，三个 callback 运行成功；
八路并发和后续重试只发生一次库存写入。隔离 Maven 仓库中的 SDK 安装完成后，
仓库外复制的消费工程在线及离线 verify 均通过。耗时不作为独立用户采用指标。

本记录没有证明真实开发者已接入，也没有证明公共 Maven 仓库已经可解析此版本。

2026-09-22 维护者复核：执行 `.\scripts\verify-adoption.ps1`，从新的临时 Maven 仓库构建
41 个未签名候选文件；仓库外消费工程在线、离线各 10 项测试通过，均输出
`writes=1 available=8 retrySame=true`。这是原包名下的迁移前基线，迁移后须重新执行。

同日包名迁移后已重新执行：复用上述隔离仓库下载缓存、clean install 新 SDK，另建仓库外消费目录；在线及离线各 10 项测试通过，输出不变。此结果仍属于维护者自动化验证。

同日 Central 验证后，Maven groupId 进一步迁至 `io.github.agentpermit4j`。再次重建制品及仓库外消费工程，在线与离线各 10 项测试通过，业务写入仍为一次；仍未执行公共仓库下载验证。

## 反馈后的选择

- 有依赖或接线阻塞：优先修复文档或最小装配。
- 有具体 DTO 或代理阻塞：用原始签名建立失败案例，只解决最主要的一项。
- 暂未收到反馈：本记录保持待试用，继续准备制品，不新增推测性的 SDK API。
