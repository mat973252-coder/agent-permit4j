# 0.5.0 发布验收

日期：2026-09-22。状态：Central 已正式发布，公共仓库独立消费验收通过。

## 发布决定

用户明确要求提交代码，并确认真人试用暂时无法安排，维护者自验无问题即可发布正式版。
因此，真人试用延期，不再阻塞 0.5.0；试用记录仍为未测量，不冒充真实采用反馈。

正式版本选定 0.5.0。Java 包名与 Maven groupId 均为 `io.github.agentpermit4j`，
Central Portal 已验证命名空间；现有仓库仍在 `mat973252-coder/agent-permit4j`。

## 验收证据

- `.\mvnw.cmd -B -ntp clean verify`：通过，214 项测试。
- 真实 Redis：Docker 官方 `redis:7.4.2-alpine` 一次性实例，容器名
  `agentpermit-release-20260922`，仅绑定本机回环随机端口，不挂载业务数据，关闭持久化。
  执行以下命令通过 5 项验收，容器已停止：

  ```powershell
  .\mvnw.cmd -B -ntp -pl agent-permit-redis -am '-Dtest=RedisResultIdempotencyGuardIT' '-Dsurefire.failIfNoSpecifiedTests=false' '-DagentPermitRedisUri=redis://127.0.0.1:45819' test
  ```

  上述端口仅是本次记录；未来重跑应重新创建一次性实例并核验端口。测试会 FLUSHDB，禁止使用共享实例。
- GitHub 私密漏洞报告已启用，API 回读 `enabled=true`，SECURITY.md 已更新。
- `.\scripts\verify-adoption.ps1 -MavenRepository <隔离仓库>`：0.5.0 候选构建成功，41 个文件齐全；仓库外消费在线和离线各 10 项测试通过，`writes=1 available=8 retrySame=true`。
- WSL 受口令保护的 GPG 密钥已配置，签名验证通过，公钥可从 Ubuntu keyserver 检索；
  本机 `central` 凭据已配置。凭据、私钥与口令均不进入仓库。
- 11 个发布模块完成测试、打包与签名。Maven 上传卡在 TLS 握手，停止后改用官方
  Publisher API 上传同一份签名包，未改变制品。
- Central 部署 `2f48f632-fbc7-4d2e-b1f4-55f16d6724f8` 校验为 `VALIDATED`，
  无错误或警告；随后正式发布请求返回 HTTP 204，最终状态为 `PUBLISHED`。
  发布后的状态 API 同时附带组件索引暂不可用提示；公共仓库 POM 已返回 HTTP 200，
  以下真实下载验收也已通过，不依赖 Portal 的组件列表判定可用性。
- 签名包 SHA-256：`6559b3203ee61748ce7a02bd2e6f2e8f8007abcab47f8ba140be4c3b35dd7761`。
- 从全新空 Maven 仓库直接下载公开 0.5.0（未执行本地 SDK install），仓库外消费
  在线与离线各 10 项测试通过，`writes=1 available=8 retrySame=true`。
  复验命令为 `mvnw.cmd -B -ntp -f <独立消费工程>/pom.xml -Dmaven.repo.local=<空仓库> verify`，
  随后同一命令增加 `-o`。使用本仓库 Wrapper，消费工程复制自 `examples/spring-ai-adoption`。

## 完成情况

1. 本版本候选构建及独立消费验收已完成。
2. 签名密钥、Central 凭据、公钥检索与签名上传已完成。
3. Central 校验通过，正式发布完成。
4. 从空 Maven 仓库下载公开版本并运行仓库外消费工程，在线与离线验收通过。
