# v0.5：独立接入验证与发布准备

2026-09-22 决策更新：用户明确暂缓真实开发者试用，授权在维护者自验通过后发布正式版。
准备版本为 0.5.0；全量测试、真实 Redis、隔离消费、签名与公开仓库消费验证仍为发布门槛。
下文 2026-09-11 规划保留为历史，试用不记作已完成，其前置发布限制由本次决定替代。

状态：执行中；独立消费示例与未签名候选构建已完成，真实开发者试用待安排。核验日期：2026-09-11（Asia/Shanghai）。
基线：本地及远端 main 为 `6a204ed`，源码版本为 `0.4.0-SNAPSHOT`。
本计划使用 v0.5 作为迭代编号；源码仍为 `0.4.0-SNAPSHOT`，正式发布版本待采用反馈后统一。

## 迭代目标

让一位没有参与 SDK 实现的 Java 开发者，仅依据文档和可获取的制品，
将三个已有 Spring AI 方法接入可信执行管线，验证审批和重试行为，
并留下可复查的接入记录。根据实际阻塞点完成至多一个 SDK 能力切片，
形成可分发、可复现的候选版本。

独立消费工程证明依赖和接线可用；独立开发者试用证明接入体验。
两项证据分别记录，自动化测试或代理模拟不能代替真实开发者的试用结果。

## 当前进展与判断依据

| 方面 | 本次核验结果 | 对下一步的影响 |
| --- | --- | --- |
| 执行管线 | 授权、动态风险、精确调用审批、幂等和安全审计已实现 | 核心语义已有可执行证据，接下来验证外部消费 |
| v0.3 | 多业务方法注册、实名审批、资源版本和策略绑定已实现 | 可以选择三个已有业务方法做采用验证 |
| v0.4 | UNKNOWN 状态、独立引用、只读下游查询和条件结算已实现 | 恢复仍是退款示例能力，不能宣称通用生产恢复 |
| 当前验证 | `verify` 通过全部 12 个 reactor 项目（含父项目），212 项测试，0 失败/错误/跳过 | 可作为下一轮回归基线；默认套件不含真实 Redis IT |
| 远端状态 | main 与本地一致；最新 GitHub Actions 构建成功 | 当前不存在待推送的业务提交 |
| 分发状态 | 远端仅有 `v0.2.0` 标签，GitHub Release 列表为空；README 使用源码本地安装 | 标签不能当作新 API 的可下载 Maven 制品 |
| 接入限制 | 参数限平铺标量，需要 `-parameters`；不发现代理/接口注解，Boot 自动配置以单 callback 为单位 | 记录这些限制是否真正阻塞采用，再决定优先级 |

Maven Central 当前可用版本未在本轮重新验证。当前仓库未配置制品发布流水线，
因此不能把源码验证成功视为已可从 Central 消费。

## 范围与工作顺序

按一位主要开发者估算为 7—10 个有效工作日；试用者排期和制品仓库权限开通时间单列。
以下工期为规划估计，不是已测结果或日历承诺。

| 顺序 | 工作包与预计投入 | 交付物 | 完成标准 |
| --- | --- | --- | --- |
| 1 | 独立消费示例，1—2 天 | `examples/spring-ai-adoption/` 独立 Maven 工程、接线说明和自动化验收 | 不继承 SDK 父 POM、不依赖 Playground、不复制 SDK 源码；只通过 Maven 制品接入三个方法 |
| 2 | 独立开发者试用，约 1 天执行与整理 | `docs/adoption/2026-09-first-integration.md`，记录原始方法签名、耗时和阻塞 | 真实试用者依照文档操作；记录维护者提示，未完成的接入如实保留 |
| 3 | 修复一个最主要阻塞点，2—3 天 | 一项可评审的修复或 API 改进，以及更新后的示例 | 有试用证据与失败测试；改进后相同案例通过，已有安全语义保持 |
| 4 | 候选制品与发布门槛，2—3 天 | 版本说明、制品构建/验证配置、消费验证、支持范围说明 | 在隔离 Maven 本地仓库中安装候选制品并编译示例；发布模块通过必需检查 |
| 5 | 回归与迭代复盘，1 天 | 验收记录、剩余阻塞清单、候选版本结论 | 全量 verify、真实 Redis 验收、独立消费与试用结果分别有证据 |

维护者负责实现、回归和候选制品；独立开发者负责实际采用试验；
仓库所有者负责发布渠道与账号条件。用户于 2026-09-11 明确选择“暂未确定，先完成示例和试用说明”；当前尚未指定试用者。
等待试用反馈时可以推进制品构建和文档准备；没有反馈不能把工作包 2 标为完成，
也不能凭猜测启动工作包 3 的扩展 API。

## 第一个可执行切片

从独立消费示例开始：选择查询、预览、写操作三个方法，使用本地确定性业务替身。
初版按已支持的平铺标量接口建立消费基线；真实试用仍保留试用者原有方法签名，
不能预先把所有 DTO 或代理改写掉，再声称不存在接入障碍。

1. 使用隔离的 Maven 本地仓库安装当前 SDK 制品，再单独构建消费工程，避免已有缓存或 reactor 依赖掩盖缺失项。
2. 先加入能失败的消费验收：只读成功；写操作无审批零副作用；精确审批后执行一次；并发重试返回相同结果。
3. 通过显式依赖和应用控制的身份、审批、幂等接线使其通过，并说明各个应用组件的职责。
4. 同层补齐参数篡改、跨租户、同审批换 key 拒绝与审计不包含原始参数/输出的关键边界。

示例不需要真实 LLM、支付账户或线上数据库。依赖首次获取可联网；测试执行不访问外部业务系统。
示例应验证现有工具经公开 API 注册和执行；不为减少配置而省略 authorizer、reviewer policy 或 trusted context。

## 接入记录与改进选择

试用记录至少包含：SDK 提交/版本、Java/Spring 版本、原始三个方法签名、
首次成功调用用时、全部三个方法完成用时、下载等待、额外配置与代码、
每次维护者提示、稳定 reason code，以及未支持的类型或代理形式。
建议体验目标为首次调用 30 分钟内、三个方法 2 小时内；这是待测目标，
不包含依赖下载时间，也不能用达到时间目标掩盖审批或身份配置缺失。

只选一项有证据的改进：

- 若独立工程主要卡在依赖或示例接线，先修依赖、文档或最小装配，不新增领域 API。
- 若原始方法被嵌套 DTO/集合阻塞，只支持实际出现的一种结构；先明确规范化、不可变快照、指纹编码和拒绝边界，再写失败测试。
- 若实际 Spring 代理或接口注解阻塞，先定义一种代理形式的发现规则和重复/未保护工具的拒绝行为，不引入全量扫描。
- gateway 或 MCP 仅在出现具体消费方及调用契约后另立切片；当前不预定为 v0.5 的必交付项。

无阻塞也属于有效结果：完成文档和制品交付，不为凑版本扩大 SDK。
DTO 与代理同时遇阻时，优先解决使试用者能完成三个方法接入的最小一项，另一项明确留待后续。

## 发布与回归门槛

- [x] 汇总 v0.3/v0.4 和本轮变化，明确新旧 API、示例参数变化及支持范围；区分 Git 标签、GitHub Release 与 Maven 制品。
- [ ] 确定候选版本和发布模块；Playground 保持示例职责，父 POM 与可复用模块的传递依赖可解析。
- [ ] 根据执行时的发布仓库要求补齐元数据、sources/Javadoc、签名与发布配置，并在 CI 生成可检查的候选产物。
- [x] 使用隔离 Maven 仓库验证候选安装和独立示例；源码 install 不冒充公共仓库解析成功。
- [x] 运行全量 Maven Wrapper verify，以及一次针对一次性 Redis 的真实适配器验收；记录各自命令和结果。
- [x] 更新 SECURITY 的受支持版本与实际可用报告渠道，核验私密报告是否启用，不把模板描述当作已启用事实。2026-09-11 API 返回 `enabled=false`；正式发布前仍需启用并再次验证。
- [ ] 候选制品准备完成后执行独立发布步骤；发布后从公共仓库再次做干净消费验证，才标记公开分发完成。

默认离线行为验收命令：

```powershell
.\mvnw.cmd -B -ntp verify
git diff --check
```

现有真实 Redis 验收命令（只针对一次性本地实例）：

```powershell
.\mvnw.cmd -B -ntp -pl agent-permit-redis -am '-Dtest=RedisResultIdempotencyGuardIT' '-Dsurefire.failIfNoSpecifiedTests=false' '-DagentPermitRedisUri=redis://localhost:6379' test
```

真实 Redis 验收应作为发布候选检查，默认测试继续保持离线、自包含。
制品仓库权限暂未就绪时，候选构建可以完成；公开分发保持未完成，不能退回未经验证的依赖坐标。

## 本轮不扩展的边界

保持 [architecture.md](../architecture.md) 中的安全契约：UNKNOWN 不自动重发，
过期 owner 不移交执行权，恢复查询不获得支付能力，不用 TTL 清理重新创建执行权。
不会仅因退款示例存在就提取通用 operation store、工作流或人工强制成功接口。

真实 JVM 崩溃恢复、生产数据库证明、保留/清理设计、通用对账历史、OPA、Kafka、
多渠道审批和多框架适配继续由明确采用需求驱动。当前 Web Playground 的退款缺口
不阻塞本轮独立 Java 接入，视觉重设计不在范围内。

## 本次规划的核验记录

- 2026-09-11：`.\mvnw.cmd -B -ntp verify` 为 BUILD SUCCESS，212 项测试全部通过；正常退款和丢失响应恢复 CLI 均成功。
- 默认测试没有执行真实 Redis IT，也没有执行真实 JVM 崩溃恢复或独立开发者试用。
- [远端最新构建](https://github.com/mat973252-coder/agent-permit4j/actions/runs/34191099407) 对应 `6a204ed`，2026-09-08 完成，结果 success。
- GitHub API 当日返回空 [Release 列表](https://github.com/mat973252-coder/agent-permit4j/releases)，open issue/PR 搜索无结果；这不能证明不存在使用者。
- `git ls-remote --tags origin` 仅返回 `v0.2.0` 标签及其解引用提交。
- 已完成与历史验收见 [v0.3](v0.3.md)、[v0.4](v0.4.md) 和 [退款接入说明](../refund-example.md)。

## 2026-09-11 实施记录

- 已建立 `zjz/v0.5-independent-adoption` 分支。生产 SDK API 与版本保持不变；变更集中在独立消费示例、构建配置与说明。
- TDD：独立消费验收先因 `InventoryApplication` / `InventoryPreview` 缺失而编译失败；补齐示例后 10 项验收通过。覆盖三个业务方法、精确审批、8 路重试、篡改/租户/可信身份、审批人、资源版本和安全审计。
- 从空的临时 Maven 仓库安装 SDK 后，通过独立 POM 消费制品；复制到仓库外的新工程在线与离线 verify 都通过，输出 `writes=1 available=8 retrySame=true`。
- 候选检查先发现 starter 缺少 Javadoc JAR；为只有依赖声明的 starter 补文档包后，41 个父 POM/库 POM/JAR/sources/Javadoc 文件齐全。脚本附 SHA-256 清单，CI 保留未签名候选产物。
- 已增加 `candidate` 与显式 `central-publish` 配置、[版本记录](../../CHANGELOG.md)、[发布说明](../releasing.md)及[试用记录表](../adoption/2026-09-first-integration.md)。签名/Portal 上传和公共仓库解析尚未执行，不标为已发布。
- 本轮全量 Maven Wrapper verify：212 项 SDK 测试通过；独立消费工程的 10 项测试单列，不混入 SDK reactor 统计。新增 workflow 尚未推送，不能声称其远端运行已通过。
- 真实 Redis 验收：本机 Docker 服务未成功就绪，改为在 WSL 中从官方 Redis 7.4.2 源码构建一次性实例，绑定 `127.0.0.1:16379`、关闭持久化；核对进程身份后执行下面命令，5 项测试全部通过。测试会清空所选 Redis 数据库，绝不能指向共享或业务实例；本次服务已用 `shutdown nosave` 关闭。

本轮主要验证命令（`$adoptionRepo` 为首次从空目录建立、随后复用下载的隔离 Maven 仓库）：

```powershell
.\mvnw.cmd -B -ntp "-Dmaven.repo.local=$adoptionRepo" verify
.\scripts\verify-adoption.ps1 -MavenRepository $adoptionRepo
.\mvnw.cmd -B -ntp "-Dmaven.repo.local=$adoptionRepo" -pl agent-permit-redis -am '-Dtest=RedisResultIdempotencyGuardIT' '-Dsurefire.failIfNoSpecifiedTests=false' '-DagentPermitRedisUri=redis://127.0.0.1:16379' test
.\mvnw.cmd -B -ntp -N '-Pcandidate,central-publish' "-Dmaven.repo.local=$adoptionRepo" help:effective-pom '-Doutput=target/central-effective-pom.xml'
git diff --check
```

发布 profile 的 effective POM 生成通过，证明配置可解析；没有实际签名、上传或 Portal 校验。
